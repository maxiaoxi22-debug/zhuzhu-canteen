# 猪猪食堂 v0.3.1 发布记录

发布日期：2026-07-17

公网地址：<https://zhuzhu-canteen.vercel.app>

## 修复内容

- 已完成心愿展示成品照快照，并标明对应饭盆菜品仍存在或已删除。
- 心愿完成动效提供“返回饭盆”和“查看已完成心愿”两个明确去向。
- 饭盆或心愿单任一推荐来源读取失败时，仍展示另一来源，不再整页不可用。
- AI 菜名候选精确关联公共菜谱，普通保存也会保留 `recipeId`。
- 照片上传增加短时签名清理凭证、数据库预留与原子认领，避免保存和清理竞态误删。
- 上传、清理和识图接口增加流式请求体上限和数据库共享限流；照片清理与识图接口同时校验同源请求。
- 增加每日过期照片回收任务与手工 `npm run uploads:sweep` 恢复命令。
- 补齐生产环境变量说明、隔离 API 测试入口，并将包版本更新为 `0.3.1`。

## 数据与部署

- 代码范围：`v0.3.0..v0.3.1`
- 迁移仅新增 `dish_photo_uploads`、`api_rate_limits` 两张表及索引，无删表或删除用户数据操作。
- 迁移前备份：`2026-07-17-before-v0.3.1.json`
- 备份 SHA-256：`969b537540ab1a8361043a5103a86a5b23a8a44925a5f4ea067314968e1f1fc2`
- 迁移前后饭盆均为 17 条；ID、名称、图片地址和创建时间逐条一致。
- 公共菜谱仍为 367 份；菜单计划仍为 13 条。
- Vercel 生产部署：`dpl_89tge5tmRKznrzhwMdBvVfH1f3cd`。
- 定时回收任务已配置 `CRON_SECRET`，授权冒烟返回 `acquired=0, deleted=0, failed=0`。

## 验证结果

- 单元/配置测试：167 个通过。
- 隔离 API 测试：34 个通过。
- 隔离数据库测试：6 个通过。
- TypeScript、ESLint 和 Next.js 生产构建通过，ESLint 无错误且无警告。
- 公网首页、菜谱搜索、心愿列表和随机推荐均通过；未授权 Cron 返回 401，跨域识图请求返回 403。
- 公网和局域网两部手机走查：`PENDING`（需要真实设备）。

## 已知限制

- 编辑菜品并替换照片时，为避免误删共享图片，旧的已保存 Blob 暂不自动回收。
- 每日回收默认每批处理 25 条；异常积压时可重复执行 `npm run uploads:sweep`。
- 本地 Qwen3-VL 4B 仍是局域网 POC；公网 Gemini 密钥当前失效时会进入手动填写兜底。
- 手机图片兼容、底部安全区及局域网访问仍需真实双机验收。
